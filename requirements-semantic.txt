# Optional: enables local semantic (embeddings) matching.
# Pulls in PyTorch (~1-2 GB RAM), so only install on a host with enough memory
# (not Render's free 512 MB tier). Then set USE_SEMANTIC=1 and run
# build_embeddings.py once.
#
#   pip install -r requirements-semantic.txt
#
sentence-transformers==5.6.0
